# WSL tools

The WSL setup, environment checks, plugin build process, and 3DS installation paths are documented in [`../../BUILD.md`](../../BUILD.md).

Main commands:

```bash
./tools/wsl/setup_3ds.sh
./tools/wsl/check_3ds_env.sh
./tools/wsl/build_plugins.sh
```
